name: Repo Self-Update

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs: 
  self-update:
    uses: humix-refactor/reusable-workflows/.github/workflows/repo-self-update.yml@main
    secrets: inherit


    
